namespace HackerRankRevisit
{
    internal class Program
    {        
        static void Main(string[] args)
        {
            Console.WriteLine("There are multiple entry points in this project. You have landed at the default entry point. Every Solution.cs file in this project has its own entry point. If required, you can redirect the entry point to the desired solution file by setting the StartupObject attribute in HackerRankRevisit.csproj file.");
        }
    }
}